# Titanic Dataset Chat Agent

This project is a chat agent built with Python, FastAPI, LangChain, and Streamlit to analyze the Titanic dataset using natural language.

## Setup Instructions

1. Add your Groq API key to the `.env` file.
2. Start the backend server:
   ```bash
   cd backend
   # Activate virtual environment
   uvicorn main:app --reload